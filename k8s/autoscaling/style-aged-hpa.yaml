# HPA for Style Aged Worker
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: style-aged-worker-hpa
  namespace: imagen
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: style-aged-worker
  
  minReplicas: 1
  maxReplicas: 10
  
  metrics:
    - type: External
      external:
        metric:
          name: pubsub.googleapis.com|subscription|num_undelivered_messages
          selector:
            matchLabels:
              resource.labels.subscription_id: style-aged-jobs-sub
        target:
          type: AverageValue
          averageValue: "2"
  
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Pods
          value: 4
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 1
          periodSeconds: 120
