# =============================================================================
# WORKERS KUSTOMIZATION
# =============================================================================
#
# CPU-only workers that call Triton Inference Server via gRPC.
# No GPU required - all ML inference happens in Triton.
#
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - upscale-worker.yaml
  - enhance-worker.yaml
  - comic-worker.yaml
  - style-aged-worker.yaml
  - background-remove-worker.yaml
