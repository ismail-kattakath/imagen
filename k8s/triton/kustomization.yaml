# =============================================================================
# TRITON INFERENCE SERVER KUSTOMIZATION
# =============================================================================
#
# Deploys Triton Inference Server with all models.
# Workers call Triton via gRPC for inference.
#
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: imagen

resources:
  - deployment.yaml
  - pod-monitoring.yaml
  - model-repository-configmap.yaml
